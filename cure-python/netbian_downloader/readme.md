**2025-02-08**研究了彼岸桌面的壁纸，现在已经完成了。可以下载**风景分类**的壁纸。

当前只能分析新版图片的链接并下载。有可能是网站改版吧，旧的图片依然是以前的方式下载（需要去往图片下载页），所以忽略了它们。

现在的下载方式只需要在图片浏览界面进行简单的字符串替换就好了。

另外 `cookie.txt` 保存了需要用到的 `cookie` —— 因为网站引入了 `5s` 盾类似的防护方案。

同时，下载进度保存在 `progress.txt` 文件中，将它删除就表示会从头开始下载咯。